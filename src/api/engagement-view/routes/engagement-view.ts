/**
 * engagement-view router — no public routes, same rationale as
 * engagement-interaction: internal bookkeeping only, exposed to clients
 * exclusively via POST /engagements/view (see
 * src/api/engagement/routes/engagement-v1.ts).
 */
export default {
  routes: [],
};
